.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
*.log
.env
data/
artifacts/
mlruns/
models/

# Raw data — never commit
olist_data/
*.csv
# ...except the committed synthetic sample dataset for the zero-config demo
!sample_data/
!sample_data/*.csv

# Secrets — never commit
PROJECT_CREDENTIALS.txt
ssh_credentials.txt

# PDFs (reports/briefs are not source)
*.pdf

# Local-only / IDE material
.idea/

# Local MLflow server state (sqlite + artifacts)
mlflow/
