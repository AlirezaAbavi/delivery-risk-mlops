# Auto-provision the Prometheus datasource. The uid must be "prometheus" to match
# the datasource references baked into grafana/dashboards/delivery_risk_prometheus.json.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
